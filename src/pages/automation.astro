---
import BaseLayout from '@/layouts/BaseLayout.astro';
import CTAButton from '@/components/CTAButton.astro';
import SectionHeading from '@/components/SectionHeading.astro';
import InfoCard from '@/components/InfoCard.astro';
import { config } from '@/config';

const title = `${config.brands.automation}｜法人AI（自動化）`;
const description =
  'AIYuMe Automation。業務の自動化・AI活用の受託。小さなPoCから段階的に進め、現場で回る形に落とし込みます。';
---

<BaseLayout title={title} description={description}>
  <!-- Hero -->
  <section class="section hero text-center">
    <div class="container">
      <span class="pill-badge">{config.brands.automation}</span>
      <h1 class="h1">
        AI自動化を、<br />
        <span class="text-brand">現場で回る形</span>に。
      </h1>
      <p class="lead text-muted">
        要件定義から運用設計まで。<br />
        小さく始めて、確実に効果が出る範囲を広げます。
      </p>
      <div class="actions justify-center">
        <a class="btn btn-primary" href="/contact">問い合わせ窓口</a>
        <CTAButton kind="line" label="LINEで軽く相談" variant="ghost" />
      </div>
    </div>
  </section>

  <!-- Areas -->
  <section class="section bg-surface">
    <div class="container">
      <SectionHeading title="Areas" center />
      <div class="grid cols-2">
        <InfoCard title="定型業務の自動化" icon="⚙️">
          <ul class="clean-list">
            <li>データ入力・集計・整形</li>
            <li>通知・アラートの自動化</li>
            <li>API連携によるワークフロー構築</li>
          </ul>
        </InfoCard>
        <InfoCard title="生成AI (LLM) 活用" icon="🧠">
          <ul class="clean-list">
            <li>社内ナレッジの検索・要約</li>
            <li>問い合わせ一時対応の半自動化</li>
            <li>ドキュメント生成支援</li>
          </ul>
        </InfoCard>
      </div>
    </div>
  </section>

  <!-- Process -->
  <section class="section compact">
    <div class="container">
      <SectionHeading title="Process" center />
      <div class="grid cols-3">
        <InfoCard title="1. ヒアリング" icon="👂">
          <p>目的・制約・現行フローを整理し、どこを自動化すべきか見定めます。</p>
        </InfoCard>
        <InfoCard title="2. PoC (試作)" icon="🧪">
          <p>短期間でプロトタイプを作成し、期待通りの挙動になるか検証します。</p>
        </InfoCard>
        <InfoCard title="3. 運用設計" icon="📦">
          <p>現場のマニュアル、エラー時の対応、権限設計を含めて納品します。</p>
        </InfoCard>
      </div>
      <div class="note text-center mt-4">
        ※セキュリティ・機密保持（NDA）については、契約前にご相談の上、適切なフローで対応します。
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="section">
    <div class="container text-center">
      <div class="card cta-box">
        <h2 class="h2">業務改善のご相談</h2>
        <p class="text-muted mb-8">
          まずは「何が手間になっているか」をお聞かせください。<br />
          自動化できる部分と、人がやるべき部分を整理します。
        </p>
        <a class="btn btn-primary" href="/contact">問い合わせる</a>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .hero {
    padding-top: 4rem;
    padding-bottom: 4rem;
  }
  .pill-badge {
    display: inline-block;
    padding: 0.25rem 0.75rem;
    border-radius: 99px;
    background: rgba(99, 102, 241, 0.1);
    color: var(--brand);
    font-size: 0.875rem;
    font-weight: 700;
    margin-bottom: 1.5rem;
    border: 1px solid rgba(99, 102, 241, 0.2);
  }
  .lead {
    font-size: 1.125rem;
    margin-bottom: 2rem;
  }
  .actions {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }
  .justify-center {
    justify-content: center;
  }
  .bg-surface {
    background: rgba(30, 41, 59, 0.3);
  }
  .clean-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  .clean-list li {
    padding: 0.25rem 0;
    color: var(--text-muted);
  }
  .cta-box {
    padding: 3rem;
  }
  .mt-4 { margin-top: 2rem; }
  .mb-8 { margin-bottom: 2rem; }
</style>
