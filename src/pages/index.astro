---
import BaseLayout from '@/layouts/BaseLayout.astro';
import CTAButton from '@/components/CTAButton.astro';
import InfoCard from '@/components/InfoCard.astro';
import SectionHeading from '@/components/SectionHeading.astro';
import { config } from '@/config';

const title = `${config.brands.home}｜受験の「続かない」を仕組みで変える`;
const description =
  'AIYuMe。難関受験の学習伴走、クリエイター支援、業務自動化の3つの事業を展開。AIYuMe EnglishはAIYuMe Learning内コンテンツとして提供します。';
---

<BaseLayout title={title} description={description}>
  <!-- Hero Portal -->
  <section class="section hero-portal">
    <div class="container text-center">
      <h1 class="h1 brand-title">
        <span class="text-brand">AI</span>YuMe
      </h1>
      <p class="lead text-muted">
        Intelligence, Organized.<br />
        知性を整理し、前へ進める。
      </p>
      <div class="portal-grid">
        <!-- Education (Main) -->
        <a href="/education" class="portal-card main-card">
          <div class="badge">Main Service</div>
          <div class="card-content">
            <h2 class="h2">{config.brands.education}</h2>
            <p class="text-muted">
              難関受験の「続かない」を仕組みで解決する。<br />
              戦略的学習伴走サービス。
              <br />
              <span class="text-dim">※AIYuMe English（英単語アプリ）を含む</span>
            </p>
            <div class="action-fake">
              <span class="btn btn-primary">詳しく見る</span>
            </div>
          </div>
        </a>

        <!-- Sub Services -->
        <div class="sub-grid">
          <a href="/creator" class="portal-card sub-card">
            <h3 class="h3">{config.brands.creator}</h3>
            <p class="text-dim text-sm">音楽制作 / 動画編集</p>
            <span class="arrow">→</span>
          </a>
          <a href="/automation" class="portal-card sub-card">
            <h3 class="h3">{config.brands.automation}</h3>
            <p class="text-dim text-sm">法人AI導入 / 自動化</p>
            <span class="arrow">→</span>
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- About -->
  <section class="section compact bg-surface">
    <div class="container">
      <SectionHeading title="Concept" subtitle="Why AIYuMe?" center />
      <div class="grid cols-2">
        <InfoCard title="構造化" icon="🧩">
          <p>
            「気合」や「感覚」に頼らず、課題を構造化して解決します。
            学習も業務も、ボトルネックを特定すれば自然と動けるようになります。
          </p>
        </InfoCard>
        <InfoCard title="技術と人の調和" icon="🤖">
          <p>
            AIやツールは徹底的に活用しますが、最終的な「意思決定」と「責任」は人が持ちます。
            システムに使われない、主体的な運用を設計します。
          </p>
        </InfoCard>
      </div>
    </div>
  </section>

  <!-- Latest / Links -->
  <section class="section">
    <div class="container text-center">
      <SectionHeading title="コンテンツ" subtitle="Contents" center />
      <div class="actions justify-center">
        <a href="/articles" class="btn btn-outline">記事一覧を見る</a>
        <a href="/contact" class="btn btn-ghost">お問い合わせ</a>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .hero-portal {
    padding-top: 6rem;
    padding-bottom: 6rem;
  }
  .brand-title {
    font-size: 4rem;
    margin-bottom: 1rem;
    letter-spacing: -0.03em;
  }
  .lead {
    margin-bottom: 4rem;
  }
  .portal-grid {
    max-width: 900px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }
  .portal-card {
    display: block;
    border: 1px solid var(--border);
    border-radius: var(--radius);
    background: rgba(30, 41, 59, 0.4);
    transition: all 0.2s;
    text-decoration: none;
    position: relative;
    overflow: hidden;
  }
  .portal-card:hover {
    border-color: var(--brand);
    background: rgba(30, 41, 59, 0.6);
    transform: translateY(-2px);
  }
  .main-card {
    padding: 3rem 2rem;
    border-color: rgba(99, 102, 241, 0.3);
    background: linear-gradient(180deg, rgba(99, 102, 241, 0.1), rgba(30, 41, 59, 0.4));
  }
  .badge {
    position: absolute;
    top: 1rem;
    right: 1rem;
    background: var(--brand);
    color: white;
    font-size: 0.75rem;
    font-weight: 700;
    padding: 0.25rem 0.75rem;
    border-radius: 99px;
  }
  .action-fake {
    margin-top: 2rem;
  }
  .sub-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
  }
  .sub-card {
    padding: 2rem;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    gap: 0.5rem;
  }
  .arrow {
    font-size: 1.5rem;
    color: var(--text-dim);
    margin-top: 1rem;
    transition: transform 0.2s;
  }
  .sub-card:hover .arrow {
    transform: translateX(4px);
    color: var(--text);
  }
  .text-sm {
    font-size: 0.875rem;
  }
  .justify-center {
    justify-content: center;
  }
  .bg-surface {
    background: rgba(30, 41, 59, 0.3);
  }

  @media (max-width: 768px) {
    .sub-grid {
      grid-template-columns: 1fr;
    }
    .brand-title {
      font-size: 3rem;
    }
  }
</style>
