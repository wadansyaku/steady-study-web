---
import BaseLayout from '@/layouts/BaseLayout.astro';
import CTAButton from '@/components/CTAButton.astro';
import SectionHeading from '@/components/SectionHeading.astro';
import InfoCard from '@/components/InfoCard.astro';
import FAQAccordion from '@/components/FAQAccordion.astro';
import { config } from '@/config';

const title = `${config.brands.education}｜難関受験の学習伴走`;
const description =
  '難関中学・高校・医学部受験向け。学習計画と振り返りを「仕組み化」し、迷いなく勉強できる状態を作ります。週1回の面談＋チャット伴走。';

const pains = [
  { text: '何から手を付けていいか分からず、手が止まる時間が長い', icon: '🛑' },
  { text: '計画を立てても、数日で崩れて自己嫌悪になる', icon: '📉' },
  { text: '模試の振り返りが活かせず、同じミスを繰り返す', icon: '🔄' },
  { text: '親子で勉強の方針について揉めてしまう', icon: '⚡' },
];

const features = [
  {
    title: '続けられる仕組み',
    desc: '今日やることを「迷いなく」始められる形に落とし込みます。負荷が上がりすぎない配分、詰まった時の立て直し方までセットで設計します。',
    icon: '🏗️',
  },
  {
    title: '個別最適化',
    desc: '教材を増やす前に、志望校と現状から必要な要素を棚卸し。「今やるべきこと」と「後回しにする勇気」を明確にします。',
    icon: '🎯',
  },
  {
    title: '週次レビュー (PDCA)',
    desc: '模試・過去問・日々のログをもとに、1週間単位でズレを修正。改善点が具体的な行動に落ちるまで伴走します。',
    icon: '📊',
  },
];

const faqs = [
  {
    q: '料金はいくらですか？',
    a: `目安は ${config.education.pricePlaceholder} です。学年・志望校・現状・支援の深さで最適が変わるため、無料面談で状況を伺ったうえでご提案します。`,
  },
  {
    q: '対象は誰ですか？',
    a: '難関中学受験・難関高校受験・医学部受験（再受験含む）を想定しています。基礎から最難関レベルまで、現状と志望に合わせて設計します。',
  },
  {
    q: '塾に通っていても併用できますか？',
    a: '可能です。むしろ塾のカリキュラムを最大限活かすために、家庭学習の優先順位と復習のサイクルを整えます。',
  },
  {
    q: '保護者はどれくらい関わればいいですか？',
    a: 'ご家庭の方針に合わせて設計します。「完全に見守り」でも、「ポイントだけ確認」でもOKです。役割分担を明確にし、親子の摩擦を減らします。',
  },
  {
    q: 'AIはどのように使いますか？',
    a: '学習記録の整理、弱点の棚卸し、類似問題の作成などで活用します。「管理の負荷」をAIで下げ、人は「方針の決定」に集中するためです。',
  },
  {
    q: '週5枠限定なのはなぜですか？',
    a: '一人ひとりのカリキュラム設計と日々の伴走に時間を使うため、品質を維持できる人数に絞っています。',
  },
];
---

<BaseLayout title={title} description={description}>
  <!-- Hero -->
  <section class="section hero-section">
    <div class="container">
      <div class="grid cols-2 items-center">
        <div>
          <span class="pill-badge">{config.education.bookingNote}</span>
          <h1 class="h1">
            受験の「続かない」を、<br />
            <span class="text-brand">仕組み</span>で解決する。<br />
            <span class="text-accent">戦略的学習伴走</span>。
          </h1>
          <p class="lead text-muted">
            週1回の面談で「計画・運用・振り返り」を整え、
            次の1週間を迷いなく実行できる状態にします。<br />
            精神論ではなく、システムで継続を支えます。
          </p>
          <div class="actions">
            <CTAButton kind="line" label="まずはご相談（LINE無料）" />
            <CTAButton kind="booking" label="空き日程を確認" variant="outline" />
          </div>
          <p class="note-sm text-dim">
            対象：難関中学 / 高校 / 医学部受験<br />
            ※無理な勧誘は一切行いません。
          </p>
        </div>

        <div class="trust-card-wrapper">
          <InfoCard title="大切にすること" class="trust-card">
            <ul class="clean-list">
              <li>✅ <strong>やることを小さく：</strong>迷いが減る粒度まで分解</li>
              <li>✅ <strong>振り返りを型化：</strong>模試が次の行動に繋がる</li>
              <li>✅ <strong>家族の平和：</strong>揉めにくい関わり方を設計</li>
            </ul>
            <div class="profile-mini">
              <div class="role text-brand">監修・伴走</div>
              <div>名古屋大学医学部 在学</div>
              <div>学習設計・教材設計専門</div>
            </div>
          </InfoCard>
        </div>
      </div>
    </div>
  </section>

  <!-- Pains -->
  <section class="section">
    <div class="container">
      <SectionHeading
        title="こんな「詰まり」を感じていませんか？"
        subtitle="Current Issues"
        center
      />
      <div class="grid cols-2">
        {
          pains.map((p) => (
            <InfoCard title="" icon={p.icon} class="pain-card">
              <p class="pain-text">{p.text}</p>
            </InfoCard>
          ))
        }
      </div>
      <div class="message-box text-center">
        <p>
          情報が足りないのではなく、<br class="mobile-hide" />
          <strong>「実行の設計（運用）」</strong>が難しいケースがほとんどです。<br />
          だからこそ、気合ではなく仕組みから整える必要があります。
        </p>
      </div>
    </div>
  </section>

  <!-- Solution -->
  <section class="section bg-surface">
    <div class="container">
      <SectionHeading
        title="AIYuMeのアプローチ"
        subtitle="Our Approach"
        center
      >
        <p slot="desc" class="text-center text-muted">
          「計画→実行→振り返り→修正」のサイクルを、<br />
          本人の性格と生活リズムに合わせて組み込みます。
        </p>
      </SectionHeading>

      <div class="grid cols-3">
        {
          features.map((f) => (
            <InfoCard title={f.title} icon={f.icon}>
              <p>{f.desc}</p>
            </InfoCard>
          ))
        }
      </div>
    </div>
  </section>

  <!-- Learning Contents -->
  <section class="section compact">
    <div class="container">
      <SectionHeading title="学習コンテンツ" subtitle="Learning Contents" center />
      <div class="card content-card">
        <h3 class="h3">{config.brands.english}（中学生向け英単語アプリ）</h3>
        <p class="text-muted">
          {config.brands.english}
          は、AIYuMe Learning の中で使う英語学習コンテンツです。ポップで続けやすいUIで、
          中学生の語彙学習を日々回せるように設計しています。
        </p>
        <div class="actions content-actions justify-center">
          <a class="btn btn-outline" href={config.urls.englishApp}>{config.brands.english}を開く</a>
        </div>
      </div>
    </div>
  </section>

  <!-- Plans -->
  <section class="section">
    <div class="container">
      <SectionHeading title="伴走プラン" subtitle="Plans" center />
      <div class="grid cols-2 plan-grid">
        {
          config.education.plans.map((plan) => (
            <div class="card plan-card">
              <div class="plan-header">
                <h3 class="h2">{plan.name}</h3>
                <div class="price text-brand">{plan.price}</div>
              </div>
              <ul class="feature-list">
                {plan.features.map((f) => (
                  <li>{f}</li>
                ))}
              </ul>
              <div class="plan-footer">
                <CTAButton
                  kind="line"
                  label="このプランを相談"
                  variant="primary"
                  class="w-full"
                />
              </div>
            </div>
          ))
        }
      </div>
      <p class="text-center text-dim mt-4">
        ※料金や支援範囲は、現状に合わせて面談で最適化・ご提案します。
      </p>
    </div>
  </section>

  <!-- Process -->
  <section class="section compact bg-surface">
    <div class="container">
      <SectionHeading title="開始までの流れ" subtitle="Process" center />
      <div class="steps-container">
        <div class="step">
          <div class="step-num">01</div>
          <div class="step-content">
            <h4>初回無料面談 (30分)</h4>
            <p>現状の成績・志望校・使っている教材を整理し、何がボトルネックになっているかを言語化します。</p>
          </div>
        </div>
        <div class="step">
          <div class="step-num">02</div>
          <div class="step-content">
            <h4>処方箋の提案</h4>
            <p>得点を伸ばすための戦略と、それを実行するための「2週間プラン」をご提案します。</p>
          </div>
        </div>
        <div class="step">
          <div class="step-num">03</div>
          <div class="step-content">
            <h4>伴走開始</h4>
            <p>週1回の面談で軌道修正しながら、自走できるサイクルを作っていきます。</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- FAQ -->
  <section class="section">
    <div class="container">
      <SectionHeading title="よくある質問" subtitle="FAQ" center />
      <div class="faq-wrapper">
        <FAQAccordion items={faqs} />
      </div>
    </div>
  </section>

  <!-- Final CTA -->
  <section class="section section-cta">
    <div class="container">
      <div class="card cta-box">
        <h2 class="h2 text-center">まずは「詰まり」の原因を整理しませんか？</h2>
        <p class="text-center text-muted mb-8">
          週5枠限定のため、枠が埋まりやすくなっています。<br />
          無理な勧誘はしません。現状の整理だけでもお役に立てます。
        </p>
        <div class="actions justify-center">
          <CTAButton kind="line" label="LINEで無料面談を予約" />
          <CTAButton kind="booking" label="Webで日程を選ぶ" variant="outline" />
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .hero-section {
    padding-top: 4rem;
    padding-bottom: 6rem;
  }
  .pill-badge {
    display: inline-block;
    padding: 0.25rem 0.75rem;
    border-radius: 99px;
    background: rgba(99, 102, 241, 0.1);
    color: var(--brand);
    font-size: 0.875rem;
    font-weight: 700;
    margin-bottom: 1.5rem;
    border: 1px solid rgba(99, 102, 241, 0.2);
  }
  .lead {
    font-size: 1.125rem;
    margin-top: 1.5rem;
    margin-bottom: 2rem;
  }
  .actions {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
    margin-bottom: 1.5rem;
  }
  .note-sm {
    font-size: 0.875rem;
  }
  .content-card {
    max-width: 760px;
    margin: 0 auto;
    text-align: center;
  }
  .content-actions {
    margin-top: 1.5rem;
    margin-bottom: 0;
  }
  .trust-card-wrapper {
    display: flex;
    justify-content: center;
  }
  .trust-card {
    border-top: 4px solid var(--accent);
  }
  .clean-list {
    list-style: none;
    padding: 0;
    margin: 0 0 1.5rem 0;
  }
  .clean-list li {
    margin-bottom: 0.75rem;
  }
  .profile-mini {
    padding-top: 1rem;
    border-top: 1px solid var(--border);
    font-size: 0.9rem;
    color: var(--text-dim);
  }
  .pain-card {
    display: flex;
    align-items: center;
    gap: 1rem;
  }
  .pain-text {
    font-weight: 700;
    font-size: 1.1rem;
    margin: 0;
  }
  .message-box {
    margin-top: 3rem;
    padding: 2rem;
    background: linear-gradient(to bottom, transparent, rgba(99, 102, 241, 0.05));
    border-radius: var(--radius);
    border: 1px solid var(--border);
  }
  .bg-surface {
    background: rgba(30, 41, 59, 0.3);
  }
  .plan-card {
    display: flex;
    flex-direction: column;
    padding: 2.5rem;
    border: 1px solid var(--border);
    background: rgba(30, 41, 59, 0.4);
  }
  .plan-header {
    margin-bottom: 2rem;
    text-align: center;
  }
  .price {
    font-size: 1.5rem;
    font-weight: 700;
  }
  .feature-list {
    list-style: none;
    padding: 0;
    margin: 0 0 2rem 0;
    flex-grow: 1;
  }
  .feature-list li {
    margin-bottom: 1rem;
    padding-left: 1.5rem;
    position: relative;
  }
  .feature-list li::before {
    content: '✓';
    color: var(--accent);
    position: absolute;
    left: 0;
    font-weight: bold;
  }
  .steps-container {
    max-width: 800px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }
  .step {
    display: flex;
    gap: 1.5rem;
    align-items: flex-start;
  }
  .step-num {
    font-size: 2rem;
    font-weight: 900;
    color: var(--brand); /* Outline style */
    opacity: 0.5;
    line-height: 1;
  }
  .step-content h4 {
    font-size: 1.2rem;
    margin-bottom: 0.5rem;
  }
  .step-content p {
    color: var(--text-muted);
    font-size: 0.95rem;
  }
  .faq-wrapper {
    max-width: 800px;
    margin: 0 auto;
  }
  .cta-box {
    background: linear-gradient(135deg, rgba(16, 185, 129, 0.1), rgba(99, 102, 241, 0.1));
    border: 1px solid rgba(16, 185, 129, 0.3);
    padding: 4rem 2rem;
  }
  .justify-center {
    justify-content: center;
  }
  @media (max-width: 768px) {
    .mobile-hide { display: none; }
    .hero-section { padding-top: 2rem; }
  }
</style>
