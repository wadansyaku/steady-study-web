{
  "name": "steady-study-web",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "cf:pages:dev": "npx wrangler pages dev dist --compatibility-date=2026-02-22",
    "cf:d1:migrate:local": "npx wrangler d1 migrations apply steady_study_voidrush_prod --local",
    "cf:d1:migrate:remote:prod": "npx wrangler d1 migrations apply steady_study_voidrush_prod --remote",
    "cf:d1:migrate:remote:preview": "npx wrangler d1 migrations apply steady_study_voidrush_prod --remote --preview",
    "cf:d1:studio:prod": "npx wrangler d1 execute steady_study_voidrush_prod --remote --command \"SELECT player_id, season_score, updated_at FROM voidrush_players ORDER BY season_score DESC LIMIT 20;\""
  },
  "dependencies": {
    "astro": "^4.16.0"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "wrangler": "^4.67.0"
  }
}
